<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns          ="http://xamarin.com/schemas/2014/forms"
             xmlns:x        ="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls ="clr-namespace:CoPiloto.Controls"
             x:Class        ="CoPiloto.Views.ChartPage"
             Title          ="{Binding Title}">

    <controls:PortableDocumentFileViewer x:Name="pdfWebView"
                                         IsVisible="{Binding IsPDF}"
                                         Source="{Binding Url}"
                                         Uri="{Binding Url}"/>

    <!--<WebView Source     ="{Binding Url}"
             x:Name     ="Chart"
             Navigating ="Chart_Navigating"/>-->
</ContentPage>